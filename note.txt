joachim8675309.medium.com/instaIling-node-js-with-nvm-4dc469c977d9
github.com/TryCatchLearn/reactivities

dotnet --info
dotnet new list
./create-projects-win.ps1

dotnet sln list
dotnet run
dotnet tool list -g

dotnet install tool -g dotnet-ef --version 8.0.0
dotnet ef
dotnet ef migrations add InitialCreate -s API -p Persistence
dotnet watch
dotnet watch --no-hot-reload run
dotnet new -l
dotnet new gitignore

git status
git init

echo "# Reactivities" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Guy-Talmi/Reactivities.git

npm create vite@latest
client-app
react

cd client-app
npm install
npm run dev

nvm install lts
nvm use lts
nvm list
nvm list available
nvm install v14.17.3
nvm use v14.17.3
node -v

 npm install semantic-ui-react semantic-ui-css

npm run build

npm install uuid
npm i --save-dev @types/uuid

npm install mobx mobx-react-lite

npm install react-router-dom

 npm install react-calendar
 npm install @types/react-calendar
 npm install react-toastify
 clear

 dotnet ef database drop -s API -p Persistence

npm install formik
npm --version

npm install yup
npm install @types/yup --save-dev
npm ls date-fns
npm install date-fns
npm install react-datepicker
npm install @types/react-datepicker --save-dev

 dotnet restore

 dotnet ef migrations add IdentityAdd -p Persistence -s API
 dotnet ef migrations add InitialCreate -s API -p Persistence
 
 dotnet ef migrations add InitialCreate -s API -p Persistence
 dotnet ef database update -s API -p Persistence
 
 dotnet ef migrations remove -s API -p Persistence
 dotnet ef migrations add ActivityAttendee -s API -p Persistence
 dotnet ef migrations add AddCancelledProperty -s API -p Persistence

 dotnet new classlib -n Infrastructure
 dotnet sln add Infrastructure
 dotnet add reference ../Application
 dotnet add reference ../Infrastructure

 dotnet ef database drop -s API -p Persistence

 dotnet watch run
 
